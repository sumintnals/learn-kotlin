클래스의 기능
- 클래스 -> 거푸집
- 거푸집을 통해서 찍어내는 과정 -> instance화
- 그 결과물 -> 객체, Object

    - val book = Book("책 제목")

- 거푸집을 사용하는 방법 -> 생성자 사용법
- 결과물이 가지고 있는 것 -> 속성, 프로퍼티(ex: 책 제목)
- 클래스를 통해서 객체를 만들고 그 객체한테 일을 시켜서 문제 해결
    - 일을 시키려면 그 일이 일을 하도록 기능을 만들어야 함

