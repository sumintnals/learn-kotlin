클래스
- 객체를 만드는 문법적인 요소
- 설명서(해당 클래스를 통해서 객체를 만드는 방법을 제시)
- 기능 설명서(객체가 수행하는 기능을 제시)

생성자
- 객체를 만드는 방법
- 주 생성자(Primary Constructor)
- 부 생성자 (Secondary Constructor)

- init 블록 (initial, 초기) 블록 -> {}
    - 초기화 블록
    - 초기화 시 필요한 작업을 하는 곳
    - 변수 선언과 마찬가지로 클래스도 생성 시 초기화를 시켜줘야 함